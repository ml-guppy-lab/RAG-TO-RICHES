----------------------------------------------------------------------------------------------
************ for my personal reference *******************************************************
----------------------------------------------------------------------------------------------

git credential reject (to initialize my personal github repo)
then paste these two lines below and hit enter twice:
protocol=https
host=github.com

---required to set my personal email for commits to show on my github commits graph---
cd /path/to/your/new/repo
git config user.email "sonalsh250@gmail.com"

initialize repository: git remote add origin https://github.com/ml-guppy-lab/fire_writing.git

git branch --set-upstream-to=origin/main main

------------------------------------------------------------------------------
VIRTUAL ENVIRONMENT
------------------------------------------------------------------------------
python3 -m venv .venv
source .venv/bin/activate

------------------------------------------------------------------------------
CREATE requirements.txt
------------------------------------------------------------------------------

------------------------------------------------------------------------------
INSTALL DEPENDENCIES
------------------------------------------------------------------------------
# NOTE: Homebrew aliases (python3 → Homebrew python3.12) override venv PATH.
# Always use the explicit venv path — do NOT use bare 'python3' or 'python'.
.venv/bin/python3 -m pip install --upgrade pip
.venv/bin/python3 -m pip install -r requirements.txt



.venv/bin/python3 scripts/puzzle.py

# Terminal 1 — backend
.venv/bin/python scripts/origami_backend.py

# Terminal 2 — frontend
cd frontend && npm run dev

for Render deployment:
Start command: uvicorn scripts.origami_backend:app --host 0.0.0.0 --port $PORT

----------------------------------------------------------------------------------
python3 dataCleanup.py -> creates a csv file called movies-dialogues.csv

----------------------------------------------------------------------------------
installing llm using ollama
----------------------------------------------------------------------------------
1. brew install ollama
2. ollama serve (steps 1 and 2 in the same terminal)
3. ollama pull qwen3:14b (different terminal) (to be done only once to install this llm locally in the system)
4. ollama run qwen3:14b (to test the model locally - give a prompt)
